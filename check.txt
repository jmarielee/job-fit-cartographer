
──────────────────────────────────────────────────────────────────────
CHECK 1 — every catalog row has a card behind it
──────────────────────────────────────────────────────────────────────
  PASS  all 38 rows resolve

──────────────────────────────────────────────────────────────────────
CHECK 2 — every card is reachable from the catalog
──────────────────────────────────────────────────────────────────────
  PASS  all 38 cards are pointed at

──────────────────────────────────────────────────────────────────────
CHECK 3 — every cited line, printed beside what the source says
──────────────────────────────────────────────────────────────────────

  base.md   (short form read as scoring.js)
        scoring.js:128     const base          = 100 * (1 - gaps.gNorm);
        scoring.js:56      return { G, total, gNorm: total > 0 ? G / total : 0 };
        scoring.js:131     let score = Math.min(REALISM_CEIL, base + effectiveBonus);
        scoring.js:210     score, base: Math.round(base), bonus,
        render.js:227      + row('Base = 100 × (1 − G/Σw)', b.base)
   ~    scoring.js:130     const effectiveBonus = bonus * (1 - base / 100);
   ~    scoring.js:176     if (score >= 75) verdict = 'Strong Candidate';
   ~    scoring.js:179     else verdict = 'Do Not Apply';

  benchmark-evaluator.md   (short form read as render.js)
        render.js:437      const bp = d.benchmarkProfile || {};
        render.js:10       const ALL_SECTION_IDS = ['section-hero','section-ledger','section-committee-
        app.js:118         ['section-benchmark','section-company','section-signals','section-shaperisk'
        index.html:179     <div class="result-section" id="section-benchmark" style="display:none">

  cap-value.md   (short form read as scoring.js)
        scoring.js:37      const CAP_VALUE     = 45;   // required-gate cap
        scoring.js:68      function _requiredCoreMissing(jdItems) {
        render.js:213      `required-gate cap 45 — ${b.capped ? 'FIRED: ' + b.capReason : 'not fired'}`
        render.js:463      noteEl.textContent = `⬡ Survivability cap engaged — ${d._brain.capReason || 

  cent-w.md   (short form read as scoring.js)
        scoring.js:33      const CENT_W   = { core: 1.0, supporting: 0.6, peripheral: 0.3 };
        scoring.js:35      const EDGE_BONUS    = { core: 12, supporting: 6, peripheral: 0, none: 0 };
        render.js:225      + row('Gap mass (G)', G)
        render.js:226      + row('Strength mass (S)', S)
   ~    scoring.js:43      const c = CENT_W[it.centrality] != null ? CENT_W[it.centrality] : CENT_W.sup
   ~    scoring.js:63      S += (CENT_W[s.centrality] != null ? CENT_W[s.centrality] : CENT_W.supportin
   ~    scoring.js:97      const cent = (CENT_W[b.centrality] || 0) - (CENT_W[a.centrality] || 0);
   ~    scoring.js:64      if (rank[s.centrality] > rank[best]) best = s.centrality;
   ~    scoring.js:71      (it.centrality === 'core' || it.centrality === 'supporting') &&
   ~    scoring.js:184     recommendation = (vote.apply >= 2 && evg === 'edge') ? 'Apply' : 'Apply with

  centrality.md   (short form read as scoring.js)
        scoring.js:260     jdItems: break the JD into real line-items. tier = required or preferred, fr
        scoring.js:32      const TIER_W   = { required: 1.0, preferred: 0.35 };

  committee-note.md   (short form read as scoring.js)
        scoring.js:194     // committeeNote: honest plain-English statement of the evaluator split when
        scoring.js:205     }
        render.js:232      + row('Committee', b.committeeNote || '—')
        scoring.js:199     committeeNote = `Committee aligned — all ${total} evaluators would advance y
        scoring.js:203     committeeNote = `Committee divided — ${vote.apply} of ${total} would advance
        render.js:293      const commSection = document.getElementById('section-committee-read');
        render.js:311      commSection.style.display = 'none';
   ~    scoring.js:213     vote, recommendation, verdict, divergent, reqMiss, committeeNote, gate, gate
   ~    scoring.js:197     if (total > 0) {

  compute-score.md   (short form read as scoring.js)
        scoring.js:124     function computeScore(model) {
        scoring.js:215     }
        scoring.js:345     const _b = computeScore({ jdItems: parsedData.jdItems, strengths: parsedData
        app.js:71          demo.survivabilityScore = _b.score;
        scoring.js:220     const systemPrompt = `You operate in two explicit modes during this analysis
        scoring.js:224     
        scoring.js:343     if (parsedData.jdItems && parsedData.jdItems.length) {
        app.js:69          const _vote = (demo.evaluators || []).map(e => ({ id: e.id, lean: (e.score ?
        scoring.js:346     parsedData.survivabilityScore = _b.score;
        scoring.js:348     parsedData.verdict            = _b.verdict;
        app.js:72          demo.recommendation     = _b.recommendation;
        app.js:74          demo._brain = _b;
        scoring.js:350     parsedData._brain = _b;
   ?    app.js:75          }
        render.js:278      const gate = d._brain && d._brain.gate;
        render.js:462      if (d._brain && d._brain.capped) {
        render.js:185      function buildScoreReceipt(d) {
        render.js:237      }
        scoring.js:349     if (_b.lowConfidence) parsedData.confidenceLevel = "low";
        scoring.js:344     const _vote = (parsedData.evaluators || []).map(e => ({ id: e.id, lean: (e.s
   ~    scoring.js:209     return {
   ~    scoring.js:214     };
   ~    scoring.js:128     const base          = 100 * (1 - gaps.gNorm);
   ~    scoring.js:129     const bonus         = EDGE_BONUS[str.best] || 0;
   ~    scoring.js:131     let score = Math.min(REALISM_CEIL, base + effectiveBonus);
   ~    scoring.js:135     const reqMiss = _requiredCoreMissing(model.jdItems);
   ~    scoring.js:141     }
   ~    scoring.js:143     // Guard 3 — confidence floor (thin inputs can't produce an extreme verdict)
   ~    scoring.js:150     if (conf === 'low') { lowConfidence = true; score = Math.max(40, Math.min(65
   ~    scoring.js:162     const gate = _identifyGate(model.jdItems);
   ~    scoring.js:167     }
   ~    scoring.js:169     score = Math.round(score);
   ~    scoring.js:171     const evg  = _edgeVsGap(str.S, gaps.G);
   ~    scoring.js:172     const vote = _tallyVote((model.evaluators || []).filter(e => e.id !== 'bench
   ~    scoring.js:175     let verdict;
   ~    scoring.js:179     else verdict = 'Do Not Apply';
   ~    scoring.js:182     let recommendation;
   ~    scoring.js:189     }
   ~    scoring.js:192     if (gate && recommendation === 'Apply') recommendation = 'Apply with Caution
   ~    scoring.js:302     const userQuery = `JOB SPECIFICATIONS:\n${jd}\n\n---\n\nCANDIDATE RESUME:\n$
   ~    scoring.js:194     // committeeNote: honest plain-English statement of the evaluator split when
   ~    scoring.js:205     }
   ~    scoring.js:207     const divergent = vote.apply >= 1 && vote.skip >= 1;
   ~    scoring.js:213     vote, recommendation, verdict, divergent, reqMiss, committeeNote, gate, gate
   ~    scoring.js:184     recommendation = (vote.apply >= 2 && evg === 'edge') ? 'Apply' : 'Apply with
   ~    scoring.js:186     recommendation = vote.apply >= 2 ? 'Apply with Caution' : 'Do Not Apply';
   ~    scoring.js:113     return 'balanced';
   ~    scoring.js:134     // Guard 1 — required-gate cap (preferred items can NEVER trigger this)

  demo-data.md   (short form read as app.js)
        app.js:54          const DEMO_DATA = {"survivabilityScore":68,"verdict":"Viable but Exposed","r
        app.js:56          function runDemo() {
        app.js:96          }
        app.js:68          if (demo.jdItems && demo.jdItems.length) {
        app.js:75          }
        app.js:137         document.getElementById('demoBtn').addEventListener('click', runDemo);
        app.js:70          const _b = computeScore({ jdItems: demo.jdItems, strengths: demo.strengths, 
        app.js:76          render(demo);
        app.js:71          demo.survivabilityScore = _b.score;
        app.js:66          document.getElementById('section-ledger').style.display = 'none';
        scoring.js:349     if (_b.lowConfidence) parsedData.confidenceLevel = "low";
   ~    app.js:73          demo.verdict            = _b.verdict;

  edge-bonus.md   (short form read as scoring.js)
        scoring.js:35      const EDGE_BONUS    = { core: 12, supporting: 6, peripheral: 0, none: 0 };
        scoring.js:58      function _computeStrength(strengths) {
        scoring.js:67      }
        scoring.js:129     const bonus         = EDGE_BONUS[str.best] || 0;
        scoring.js:131     let score = Math.min(REALISM_CEIL, base + effectiveBonus);
        scoring.js:210     score, base: Math.round(base), bonus,
        render.js:228      + row('Edge bonus', '+' + b.bonus + ' max, scaled by headroom')
        scoring.js:39      const TIE_FACTOR    = 1.3;  // edge-vs-gap dominance threshold
        app.js:54          const DEMO_DATA = {"survivabilityScore":68,"verdict":"Viable but Exposed","r
        render.js:212      `realism ceiling 90 — always on`,
        render.js:214      `core-gate ceiling 74 — ${b.gateCapped ? 'FIRED: live core required gate' : 
   ~    scoring.js:63      S += (CENT_W[s.centrality] != null ? CENT_W[s.centrality] : CENT_W.supportin
   ~    scoring.js:64      if (rank[s.centrality] > rank[best]) best = s.centrality;
   ~    scoring.js:128     const base          = 100 * (1 - gaps.gNorm);
   ~    scoring.js:130     const effectiveBonus = bonus * (1 - base / 100);
   ~    scoring.js:176     if (score >= 75) verdict = 'Strong Candidate';
   ~    scoring.js:179     else verdict = 'Do Not Apply';
   ~    scoring.js:183     if (score >= 55) {
   ~    scoring.js:189     }
   ~    scoring.js:139     if (score > CAP_VALUE) score = CAP_VALUE;
   ~    scoring.js:150     if (conf === 'low') { lowConfidence = true; score = Math.max(40, Math.min(65
   ~    scoring.js:165     score = GATE_CORE_CEIL;
   ~    scoring.js:62      if (!s.mapsToNeed) return;

  edge-vs-gap.md   (short form read as scoring.js)
        scoring.js:110     function _edgeVsGap(S, G) {
        scoring.js:114     }
        scoring.js:171     const evg  = _edgeVsGap(str.S, gaps.G);
        scoring.js:184     recommendation = (vote.apply >= 2 && evg === 'edge') ? 'Apply' : 'Apply with
        scoring.js:212     edgeVsGap: evg, capped, capReason, lowConfidence,
        render.js:207      const domWord = b.edgeVsGap === 'edge' ? 'strengths dominate'
        render.js:209      const domTest = `S ${S} vs 1.3×G ${(1.3 * G).toFixed(2)} → ${domWord}`;
        render.js:219      + `${b.edgeVsGap === 'edge' ? 'edge dominance' : 'no edge dominance'}`
        scoring.js:35      const EDGE_BONUS    = { core: 12, supporting: 6, peripheral: 0, none: 0 };
        render.js:208      : b.edgeVsGap === 'gap'  ? 'gaps dominate' : 'balanced';
        scoring.js:39      const TIE_FACTOR    = 1.3;  // edge-vs-gap dominance threshold
        render.js:212      `realism ceiling 90 — always on`,
        render.js:214      `core-gate ceiling 74 — ${b.gateCapped ? 'FIRED: live core required gate' : 
   ~    scoring.js:169     score = Math.round(score);
   ~    scoring.js:129     const bonus         = EDGE_BONUS[str.best] || 0;
   ~    scoring.js:176     if (score >= 75) verdict = 'Strong Candidate';
   ~    scoring.js:179     else verdict = 'Do Not Apply';
   ~    scoring.js:186     recommendation = vote.apply >= 2 ? 'Apply with Caution' : 'Do Not Apply';
   ~    scoring.js:112     if (G > TIE_FACTOR * S) return 'gap';
   ~    scoring.js:192     if (gate && recommendation === 'Apply') recommendation = 'Apply with Caution

  evidence-ledger.md   (short form read as render.js)
        render.js:555      /* ═════════════════════════════════════════════════════════════════════════
        render.js:680      }
        index.html:140     <div class="result-section" id="section-ledger" style="display:none">
        index.html:163     </div>
        render.js:556      EVIDENCE LEDGER — the human-audit stage between labeling and scoring.
        render.js:559      computeScore() locally — no API call, no drift, no surprises.
        render.js:663      function renderLedgerSection() {
        app.js:136         document.getElementById('saveKeyBtn').addEventListener('click', saveApiKey);
        app.js:140         document.getElementById('openerCopyBtn').addEventListener('click', copyOpene
        render.js:597      function renderLedgerRows() {
        index.html:327     <script src="scoring.js"></script>
        index.html:331     <script src="app.js"></script>
        render.js:678      section.style.display = '';
        render.js:185      function buildScoreReceipt(d) {
        index.html:162     <p class="ledger-footnote">Three independent readings labeled this evidence;

  guard-numbering.md   (short form read as scoring.js)
        scoring.js:164     if (gate && gate.centrality === 'core' && gate.status === 'missing' && score

  jd-items.md   (short form read as scoring.js)
        scoring.js:302     const userQuery = `JOB SPECIFICATIONS:\n${jd}\n\n---\n\nCANDIDATE RESUME:\n$
        scoring.js:342     // ── deterministic scoring: the model labelled; the number is COMPUTED ──

  ledger-state-layer.md   (short form read as render.js)
        render.js:600      const L = RUN.ledger;
        render.js:677      updateLedgerPreview(LAST_REPORT && LAST_REPORT._brain ? LAST_REPORT._brain :
        index.html:327     <script src="scoring.js"></script>
        index.html:331     <script src="app.js"></script>
        render.js:656      if (brain.gateCapped)   notes.push(`⬡ ${GATE_CORE_CEIL} core-gate ceiling en
        render.js:657      if (brain.gate && !brain.gateCapped) notes.push(`⚠ live gate: ${brain.gate.t
        app.js:70          const _b = computeScore({ jdItems: demo.jdItems, strengths: demo.strengths, 

  maps-to-need.md   (short form read as scoring.js)
        scoring.js:302     const userQuery = `JOB SPECIFICATIONS:\n${jd}\n\n---\n\nCANDIDATE RESUME:\n$
        scoring.js:62      if (!s.mapsToNeed) return;
        scoring.js:263     strengths: the candidate real strengths for this role, each with centrality 

  obtainable.md   (short form read as scoring.js)
        scoring.js:261     obtainable: true in two cases — (1) the JD explicitly offers the credential 
        scoring.js:53      if (it.obtainable && it.status === 'missing') sf = STATUS_F.partial;

  recommendation.md   (short form read as scoring.js)
        scoring.js:181     // recommendation: score is the floor; committee split can add caution but c
        scoring.js:189     }
        render.js:268      document.getElementById('scoreRecText').textContent = d.recommendation || '—
        render.js:45       function recClass(rec) {
        render.js:52       function initials(name) {
        render.js:329      // Drafted Opener — ready-to-send artifact; never shown for Do Not Apply
        render.js:339      }
        scoring.js:254     DRAFTED OPENER — draftedOpener field (Mode 2): A ready-to-send outreach open
        scoring.js:175     let verdict;
        scoring.js:179     else verdict = 'Do Not Apply';
   ~    scoring.js:192     if (gate && recommendation === 'Apply') recommendation = 'Apply with Caution
   ~    scoring.js:213     vote, recommendation, verdict, divergent, reqMiss, committeeNote, gate, gate
   ~    scoring.js:169     score = Math.round(score);
   ~    scoring.js:188     recommendation = 'Do Not Apply';
   ~    scoring.js:86      const candidates = (jdItems || []).filter(it =>
   ~    scoring.js:91      );

  render.md   (short form read as render.js)
        render.js:241      function render(d) {
        render.js:536      }
        scoring.js:352     render(parsedData);
        app.js:76          render(demo);
        render.js:153      function revealSections() {
        app.js:100         document.getElementById('section-ledger').style.display = 'none';
        app.js:108         ['scoreAngle','scoreHook',
        index.html:140     <div class="result-section" id="section-ledger" style="display:none">
        app.js:66          document.getElementById('section-ledger').style.display = 'none';
        render.js:663      function renderLedgerSection() {
        scoring.js:176     if (score >= 75) verdict = 'Strong Candidate';
        scoring.js:179     else verdict = 'Do Not Apply';
   ~    render.js:56       function showIf(el, on) {
   ~    render.js:293      const commSection = document.getElementById('section-committee-read');
   ~    render.js:311      commSection.style.display = 'none';
   ~    render.js:314      // Access Strategy Path
   ~    render.js:327      }
   ~    render.js:329      // Drafted Opener — ready-to-send artifact; never shown for Do Not Apply
   ~    render.js:339      }
   ~    render.js:341      // Company Topology
   ~    render.js:354      }
   ~    render.js:366      showIf(document.getElementById('section01'), credList.length > 0 || riskList
   ~    render.js:368      // Critical Signal Deficits
   ~    render.js:388      }
   ~    render.js:417      // Shape Risks
   ~    render.js:434      }
   ~    render.js:436      // Market Calibration Targets
   ~    render.js:485      }
   ~    render.js:277      const gateFlag = document.getElementById('gateFlag');
   ~    render.js:287      }
   ~    render.js:290      buildScoreReceipt(d);
   ~    render.js:535      initCollapsibles();
   ~    render.js:278      const gate = d._brain && d._brain.gate;
   ~    render.js:462      if (d._brain && d._brain.capped) {
   ~    render.js:190      if (!b) { if (box) box.style.display = 'none'; return; }
   ~    render.js:170      function initCollapsibles() {
   ~    render.js:39       function scoreClass(n) {
   ~    render.js:44       }
   ~    render.js:446      const THE_BAR = 75;

  score-receipt.md   (short form read as render.js)
        render.js:185      function buildScoreReceipt(d) {
        render.js:290      buildScoreReceipt(d);
        render.js:209      const domTest = `S ${S} vs 1.3×G ${(1.3 * G).toFixed(2)} → ${domWord}`;
        render.js:214      `core-gate ceiling 74 — ${b.gateCapped ? 'FIRED: live core required gate' : 
        render.js:656      if (brain.gateCapped)   notes.push(`⬡ ${GATE_CORE_CEIL} core-gate ceiling en
        render.js:657      if (brain.gate && !brain.gateCapped) notes.push(`⚠ live gate: ${brain.gate.t

  section-reveal.md   (short form read as render.js)
        render.js:10       const ALL_SECTION_IDS = ['section-hero','section-ledger','section-committee-
        render.js:153      function revealSections() {
        render.js:162      }
        scoring.js:359     revealSections();
        app.js:93          revealSections();
        scoring.js:277     ALL_SECTION_IDS.forEach(id => {
        scoring.js:278     document.getElementById(id).classList.remove('revealed');
        app.js:122         ALL_SECTION_IDS.forEach(id => {
        app.js:124         if (el) el.classList.remove('revealed');
        render.js:241      function render(d) {
        render.js:535      initCollapsibles();
        index.html:140     <div class="result-section" id="section-ledger" style="display:none">
        app.js:66          document.getElementById('section-ledger').style.display = 'none';
        app.js:100         document.getElementById('section-ledger').style.display = 'none';
        app.js:123         const el = document.getElementById(id);
   ~    render.js:159      if (i === 0) el.scrollIntoView({ behavior:'smooth', block:'start' });
   ~    render.js:157      if (!el || el.style.display === 'none') return;

  status-f.md   (short form read as scoring.js)
        scoring.js:34      const STATUS_F = { missing: 1.0, partial: 0.5, meets: 0.0 };
        scoring.js:53      if (it.obtainable && it.status === 'missing') sf = STATUS_F.partial;
        render.js:225      + row('Gap mass (G)', G)
   ~    scoring.js:51      let sf = STATUS_F[it.status] != null ? STATUS_F[it.status] : 0;
   ~    scoring.js:54      G += w * sf;
   ~    scoring.js:72      it.status === 'missing' &&
   ~    scoring.js:89      (it.status === 'missing' || it.status === 'partial') &&
   ~    scoring.js:42      const t = TIER_W[it.tier] != null ? TIER_W[it.tier] : TIER_W.required;

  status.md   (short form read as scoring.js)
        scoring.js:260     jdItems: break the JD into real line-items. tier = required or preferred, fr
        scoring.js:33      const CENT_W   = { core: 1.0, supporting: 0.6, peripheral: 0.3 };

  strengths.md   (short form read as scoring.js)
        scoring.js:263     strengths: the candidate real strengths for this role, each with centrality 
        scoring.js:58      function _computeStrength(strengths) {

  system-prompt.md   (short form read as scoring.js)
        scoring.js:220     const systemPrompt = `You operate in two explicit modes during this analysis
        scoring.js:223     When assigning tier, centrality, status, and mapsToNeed, you are a neutral, 
        scoring.js:342     // ── deterministic scoring: the model labelled; the number is COMPUTED ──
        scoring.js:348     parsedData.verdict            = _b.verdict;

  the-gate.md   (short form read as scoring.js)
        scoring.js:76      /* ── THE GATE ────────────────────────────────────────────────────────────
        scoring.js:83      "stated but not demonstrated" is exactly how candidates get screened out. */
        render.js:277      const gateFlag = document.getElementById('gateFlag');
        render.js:287      }
        scoring.js:68      function _requiredCoreMissing(jdItems) {
        scoring.js:75      }
   ~    scoring.js:84      function _identifyGate(jdItems) {
   ~    scoring.js:103     }
   ~    scoring.js:162     const gate = _identifyGate(model.jdItems);
   ~    scoring.js:164     if (gate && gate.centrality === 'core' && gate.status === 'missing' && score
   ~    scoring.js:167     }
   ~    scoring.js:192     if (gate && recommendation === 'Apply') recommendation = 'Apply with Caution
   ~    scoring.js:213     vote, recommendation, verdict, divergent, reqMiss, committeeNote, gate, gate
   ~    scoring.js:86      const candidates = (jdItems || []).filter(it =>
   ~    scoring.js:91      );
   ~    scoring.js:94      candidates.sort((a, b) => {
   ~    scoring.js:102     return { text: g.text, status: g.status, centrality: g.centrality };
   ~    scoring.js:176     if (score >= 75) verdict = 'Strong Candidate';
   ~    scoring.js:179     else verdict = 'Do Not Apply';
   ~    scoring.js:160     // score in the base math. A supporting gate never caps. A gate NEVER rescue
   ~    scoring.js:161     // a weak score.
   ~    scoring.js:87      it.tier === 'required' &&

  tier-w.md   (short form read as scoring.js)
        scoring.js:32      const TIER_W   = { required: 1.0, preferred: 0.35 };
        scoring.js:42      const t = TIER_W[it.tier] != null ? TIER_W[it.tier] : TIER_W.required;
        scoring.js:264     Framing: a PREFERRED gap is friction to neutralize, never a disqualifier. Ne
        scoring.js:33      const CENT_W   = { core: 1.0, supporting: 0.6, peripheral: 0.3 };
        render.js:225      + row('Gap mass (G)', G)
        render.js:227      + row('Base = 100 × (1 − G/Σw)', b.base)
   ~    scoring.js:41      function _itemWeight(it) {
   ~    scoring.js:45      }
   ~    scoring.js:44      return t * c;
   ~    scoring.js:49      const w = _itemWeight(it);
   ~    scoring.js:50      total += w;
   ~    scoring.js:99      return _itemWeight(b) - _itemWeight(a);
   ~    scoring.js:43      const c = CENT_W[it.centrality] != null ? CENT_W[it.centrality] : CENT_W.sup
   ~    scoring.js:63      S += (CENT_W[s.centrality] != null ? CENT_W[s.centrality] : CENT_W.supportin
   ~    scoring.js:97      const cent = (CENT_W[b.centrality] || 0) - (CENT_W[a.centrality] || 0);
   ~    scoring.js:70      it.tier === 'required' &&
   ~    scoring.js:87      it.tier === 'required' &&

  tier.md   (short form read as scoring.js)
        scoring.js:260     jdItems: break the JD into real line-items. tier = required or preferred, fr
        scoring.js:32      const TIER_W   = { required: 1.0, preferred: 0.35 };

  verdict.md   (short form read as scoring.js)
        scoring.js:175     let verdict;
        scoring.js:179     else verdict = 'Do Not Apply';
        render.js:256      document.getElementById('scoreVerdict').textContent = d.verdict || '—';
        render.js:647      document.getElementById('ledgerScoreVerdict').textContent = brain.verdict;
        render.js:39       function scoreClass(n) {
        render.js:44       }
        render.js:40       if (n >= 75) return 'hi';
        render.js:42       if (n >= 35) return 'lo';
        render.js:446      const THE_BAR = 75;
        scoring.js:176     if (score >= 75) verdict = 'Strong Candidate';
        render.js:234      + row('Final score', b.score + ' / 100')
        render.js:212      `realism ceiling 90 — always on`,
        render.js:214      `core-gate ceiling 74 — ${b.gateCapped ? 'FIRED: live core required gate' : 
   ~    scoring.js:213     vote, recommendation, verdict, divergent, reqMiss, committeeNote, gate, gate
   ~    scoring.js:174     // verdict follows score bands only — committee split is nuance, not a veto
   ~    scoring.js:183     if (score >= 55) {
   ~    scoring.js:185     } else if (score >= 45) {
   ~    scoring.js:38      const GATE_CORE_CEIL = 74;  // a live CORE required gate can't read as "Stro

  vote-threshold.md   (short form read as scoring.js)
        scoring.js:344     const _vote = (parsedData.evaluators || []).map(e => ({ id: e.id, lean: (e.s
        app.js:69          const _vote = (demo.evaluators || []).map(e => ({ id: e.id, lean: (e.score ?
        scoring.js:237     DIVERGENCE ALERT: When the candidate's jdItem labels suggest they clear the 
        render.js:35       if (v >= 65) return { label: 'Would advance', cls: 'advance' };
        render.js:36       if (v >= 50) return { label: 'On the fence', cls: 'fence' };
        scoring.js:115     function _tallyVote(evaluators) {
        scoring.js:123     }
        render.js:33       function evalVerdict(score) {
        render.js:38       }
        scoring.js:203     committeeNote = `Committee divided — ${vote.apply} of ${total} would advance
        render.js:298      // Count strip removed; per-evaluator rows kept — the read names WHO is in t
        render.js:305      + `<span class="eval-verdict-badge ${verdict.cls}">${esc(verdict.label)}</sp
        render.js:218      const routing = `score ${b.score} + ${b.vote.apply}/3 apply leans + `
        scoring.js:118     const l = (e.lean || '').toLowerCase();
        scoring.js:120     else if (l.indexOf('apply') !== -1) apply++;
   ~    scoring.js:184     recommendation = (vote.apply >= 2 && evg === 'edge') ? 'Apply' : 'Apply with
   ~    scoring.js:186     recommendation = vote.apply >= 2 ? 'Apply with Caution' : 'Do Not Apply';

  vote.md   (short form read as scoring.js)
        scoring.js:115     function _tallyVote(evaluators) {
        scoring.js:123     }
        render.js:218      const routing = `score ${b.score} + ${b.vote.apply}/3 apply leans + `
        scoring.js:344     const _vote = (parsedData.evaluators || []).map(e => ({ id: e.id, lean: (e.s
        app.js:69          const _vote = (demo.evaluators || []).map(e => ({ id: e.id, lean: (e.score ?
        scoring.js:237     DIVERGENCE ALERT: When the candidate's jdItem labels suggest they clear the 
   ~    scoring.js:172     const vote = _tallyVote((model.evaluators || []).filter(e => e.id !== 'bench
   ~    scoring.js:184     recommendation = (vote.apply >= 2 && evg === 'edge') ? 'Apply' : 'Apply with
   ~    scoring.js:186     recommendation = vote.apply >= 2 ? 'Apply with Caution' : 'Do Not Apply';
   ~    scoring.js:195     const total = vote.apply + vote.skip;
   ~    scoring.js:207     const divergent = vote.apply >= 1 && vote.skip >= 1;
   ~    scoring.js:213     vote, recommendation, verdict, divergent, reqMiss, committeeNote, gate, gate
   ~    scoring.js:181     // recommendation: score is the floor; committee split can add caution but c
   ~    scoring.js:205     }
   ~    scoring.js:176     if (score >= 75) verdict = 'Strong Candidate';
   ~    scoring.js:179     else verdict = 'Do Not Apply';
   ~    scoring.js:118     const l = (e.lean || '').toLowerCase();
   ~    scoring.js:120     else if (l.indexOf('apply') !== -1) apply++;

  PASS  358 citations printed — none point outside their file
  WARN  1 land on a blank line or a lone brace — check those first
        145 marked ~ had no filename and were resolved by card anchor.

        Read the list. A citation can sit inside the file and still be
        wrong; that is the one thing this check cannot decide for you.
        Re-run with --suspicious for only the ? and ~ rows.

──────────────────────────────────────────────────────────────────────
CHECK 4 — demo-data.md states the score the code actually returns
──────────────────────────────────────────────────────────────────────
        computeScore returns 64 (Viable but Exposed)
        DEMO_DATA ships the literal 68, which is overwritten at run time
  PASS  demo-data.md states 64

══════════════════════════════════════════════════════════════════════
ALL CHECKS PASS  (1 skipped)
══════════════════════════════════════════════════════════════════════

